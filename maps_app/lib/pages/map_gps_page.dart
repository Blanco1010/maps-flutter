import 'package:flutter/material.dart';

class MapGpsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MapGpsPage'),
      ),
    );
  }
}
